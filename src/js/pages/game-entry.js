/**
 * Entry point for lyrics-game.html.
 * Imports the game styles and the existing game logic.
 */
import '../../styles/game.css';

// Import the existing game.js — Vite will bundle it
import '../../../game.js';

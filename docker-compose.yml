version: '3'

services:
  website:
    image: nginx:alpine
    restart: always
    networks:
      - default

networks:
  default:
    external: true
    name: clav  # Connects to your Nginx Proxy Manager
